"use client";import {useMemo,useState} from "react";import {Search,SlidersHorizontal} from "lucide-react";import SiteHeader from "@/components/SiteHeader";import BookCard from "@/components/BookCard";import {books,genres} from "@/lib/books";import Link from "next/link";
export default function Discover(){const[q,setQ]=useState("");const[g,setG]=useState("All");const list=useMemo(()=>books.filter(b=>(g==="All"||b.genre===g)&&`${b.title} ${b.author}`.toLowerCase().includes(q.toLowerCase())),[q,g]);return <><SiteHeader/><main className="mx-auto max-w-7xl px-5 py-14 sm:px-8 lg:py-20"><div className="max-w-3xl"><p className="eyebrow">The collection</p><h1 className="section-title mt-2">Find a book that meets you where you are.</h1><p className="mt-6 text-base leading-7 text-black/50">Browse the StoryBook shelf by mood, genre or author. No endless feeds — just books worth opening.</p></div><div className="mt-10 flex flex-col gap-3 md:flex-row"><div className="relative flex-1"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-black/30" size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search titles or authors" className="w-full rounded-full border border-black/10 bg-white/70 py-4 pl-12 pr-4 outline-none"/></div><div className="flex items-center gap-2 overflow-x-auto"><SlidersHorizontal size={17}/>{genres.map(x=><button key={x} onClick={()=>setG(x)} className={`whitespace-nowrap rounded-full px-4 py-2.5 text-xs font-bold ${g===x?"bg-[#171614] text-white":"border border-black/10 bg-white/60"}`}>{x}</button>)}</div></div><div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{list.map(b=><Link href={`/books/${b.id}`} key={b.id}><BookCard book={b}/></Link>)}</div>{!list.length&&<div className="paper-card mt-10 p-12 text-center"><p className="font-black">Nothing found.</p><p className="mt-2 text-sm text-black/40">Try another title, author or genre.</p></div>}</main></>}
