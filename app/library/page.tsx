"use client";import {useEffect,useMemo,useState} from "react";import {BookMarked,CheckCircle2,Clock3,Heart} from "lucide-react";import SiteHeader from "@/components/SiteHeader";import {books} from "@/lib/books";import Link from "next/link";
export default function Library(){const[saved,setSaved]=useState<string[]>([]);useEffect(()=>setSaved(JSON.parse(localStorage.getItem("storybook-saved")||"[]")),[]);const list=useMemo(()=>books.filter(b=>saved.includes(b.id)),[saved]);return <><SiteHeader/><main className="mx-auto max-w-7xl px-5 py-14 sm:px-8 lg:py-20"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="eyebrow">Your reading life</p><h1 className="section-title mt-2">My library.</h1><p className="mt-5 text-sm text-black/45">Your saved books live here on this device.</p></div><div className="flex gap-2"><span className="rounded-full bg-white/70 px-4 py-2 text-xs font-bold"><BookMarked size={14} className="mr-1 inline"/> {list.length} saved</span><span className="rounded-full bg-white/70 px-4 py-2 text-xs font-bold"><Clock3 size={14} className="mr-1 inline"/> 14 day streak</span></div></div>{list.length?<div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{list.map(b=><Link href={`/books/${b.id}`} key={b.id}><div className="paper-card p-5"><div className="mb-5 grid h-32 place-items-center rounded-2xl bg-[#e8e0d2] text-center"><b className="serif max-w-[80%] text-xl">{b.title}</b></div><p className="text-xs text-black/40">{b.author}</p><h3 className="mt-1 font-black">{b.genre}</h3><div className="mt-5 flex justify-between text-xs text-black/40"><span><CheckCircle2 size={13} className="mr-1 inline"/> Saved</span><Heart size={14}/></div></div></Link>)}</div>:<div className="paper-card mt-10 p-14 text-center"><BookMarked className="mx-auto mb-5" size={30}/><h2 className="text-xl font-black">Your shelf is waiting.</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-black/45">Save a book from Discover and it will appear here.</p><Link href="/discover" className="mt-6 inline-block rounded-full bg-[#171614] px-5 py-3 text-sm font-bold text-white">Discover books</Link></div>}</main></>}
